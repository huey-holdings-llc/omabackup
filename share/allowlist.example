# Paths under $HOME that omabackup backs up, one per line, relative to $HOME.
# Prefix a line with ? to mark it optional: a missing optional entry is skipped, a missing required entry halts the snapshot.
