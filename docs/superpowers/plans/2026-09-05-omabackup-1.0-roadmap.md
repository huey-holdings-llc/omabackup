# OmaBackup after 0.7.0: what is left for 1.0

0.7.0 ships everything the five-lens expert review put above the cut line, plus
everything the three fix rounds on top of it found. This file is the remainder:
the items the review itself marked "later", the minors nobody blocked on, and
the follow-ups the wave reviews deferred while fixing something next to them.
Each is real, each is survivable in a first public release, and each is better
as its own pull request with its own review than folded into a release branch.

One line each, with the lens or the round that raised it. Row numbers refer to
the deduplicated table in the review synthesis; the per-lens reports carry the
evidence and a fix sketch for every one.

## Landed since this list was written (updated 2026-09-13)

Fifteen items are done, each on its own pull request with a Codex review
where the change earned one. They are struck from the lists below rather
than left to be re-read:

- Row 34, the popup's rough edges (usability I3, I5, I6, I16, I17): #13.
- Row 37, three states, two glyphs (usability I14): #13.
- Row 39, the suite takes twelve minutes (architecture I8): #11.
- `.gitleaksignore` from the data repo is honoured (security M1): #12, documented as the one deliberate exception.
- A failing `find` in the filename gate reads as clean (security M3): #5.
- `/etc` reference copies are staged 0644 (security M5): #12.
- ERROR rows are counted as drift (usability M4): #13.
- `preview.png` is referenced by the README and absent from the tree: 0.7.0 release.
- `verify` misreports on a fresh clone of an adopted repo (Task 22 Step 1, 2026-09-07): #4.
- `setup --import` does not add the plugin's own config to the lists (Task 22 Step 2): #6, the config is ignored, not backed up.
- A gitleaks false positive has no documented outlet (Task 22 Step 1): #12, and one fingerprint now satisfies both scans.
- The gitignore sync leaves an uncommitted edit nobody commits (Task 22 Step 2): #3.
- The popup's key-hint footer clips at the default width (preview.png, 2026-09-07): 0.7.0 release, the row wraps.
- A snapshot that keeps failing is invisible in the popup (owner, 2026-09-07, found the hard way): #9.
- "N uncommitted" the popup offers no way to clear (owner, 2026-09-07): #10.

A second round, also 2026-09-13, is the 0.8.0 release itself: nine pull
requests plus this bump. Between them they close out everything still
marked "later" below except Row 40 and the edit-distance note, which are
kept for 1.0, and they also close the three things the first round's work
found and left open:

- The drift counter: `H_DRIFT_COUNT` and the snapshot's own count no longer disagree on TOOBIG and EXCLUDED rows: #14.
- Row 23, one reply layer: every triage verb and every `problems` list answers in one shape instead of three, and a broken repo gets a degraded status instead of a stale one: #15.
- Row 30, the push probe: an inconclusive verdict now carries the date it last actually settled the question, instead of losing it on every retry: #16.
- Rows 33 and 35, usability M3, and the Wave C restore item: `setup check` is a doctor, every verb takes `--help`, `setup --remove` names `--yes` when it cancels, the divergence notification stops asking a non-developer for `pull --rebase`, triage buttons carry accessible names, and restore's dry run covers every stage (the jq-fallback half of the restore item is moot now that jq is required at startup): #17.
- Round 2 R6, the `/etc` scan: it asks pacman for one field at a time instead of parsing its prose: #18.
- Sorted manifests, a red group 04 that names what moved, and `lint`'s `cp -Pp`, closing the group 04 idempotency risk the first round's work found: #19.
- Rows 31 and 12, Round 3 concern 2, and Wave C item 6: the allowlist floor follows the last committed list with an `--accept-allowlist` escape hatch, one cleanup registry replaces the two competing `EXIT` traps, the vanish guard no longer writes a scratch file, and a `[` in a filename is no longer refused: #20.
- Wave B concern 1, two fewer forks per process on the hot path: #21.
- Row 41, the structural cleanups: one setup phase table, one verb list, the misplaced functions moved home, restore's globals out of `lib/verify.sh`, config merging down to one shape, and `Panel.qml` reading service state instead of re-deriving it: #22.

`AGENTS.md` being gitignored, so the Codex bot reviewing on GitHub never
read the review priorities written in it, is resolved a different way
rather than fixed in code: the review request comment posted on each PR now
carries those priorities directly. The CHANGELOG comparison links land with
this same bump.

**What the second round found.** Codex found a P0 in three of these ten
pull requests, all accepted: the `/etc` ownership check twice, before #18
fixed it; the allowlist floor three times, before #20 held; and the
absent-`systemd-analyze` case once, on #21. Both pacman ownership guards had
been reading a silent non-zero exit from pacman as "owned" until #18 closed
that. The verb table's `none` and `special` load modes turned out to be
documentation rather than anything dispatch actually enforces, which is left
as a follow-up. GitHub Actions was locked out account-wide on 2026-09-13, so
the last four of these pull requests merged on the local suite and a Docker
replica of the CI container instead of on GitHub CI. The fix wave's own
scanned-commit helper went through two Codex passes that each found a way
for an unscanned change to reach the commit, first the worktree contents via
a pathspec, then the whole index, which is why it now commits a tree id.

## Deferred by the review (rows marked "later")

- **Row 40, status.json has no schema version (architecture I9).** Several of
  its two dozen fields have no reader, every read in QML is defensive, and a
  renamed field therefore reads as zero drift rather than as an error. The Wave C
  fix round deferred this explicitly while adding `dir` and `optional`, two
  fields whose meaning depends on a row's `type`, which is exactly the shape a
  schema version exists to make safe.
- **`snapshot_commit` and `cmd_push --confirm` still commit the whole index
  (second Codex review of #23, deferred by ruling).** The four commits the
  tool makes for you (the accept-allowlist commit, the .gitignore top-up,
  setup's initial layout and the adoption marker) now go through
  `repo_commit_scanned`, which pins the scanned tree and commits it by id.
  These two still scan and then run a bare `git commit` of the whole index
  inside the lock, so a `git add` by another process in that window is the
  same exposure 0.7.0 had; give them the same treatment at 1.0.

## Raised by the wave reviews and fix rounds

- **Edit distance 2 is generous on short config keys (Wave B concern 3).** A
  future `timers` or `notifys` would be refused as a typo of `timer` or `notify`
  rather than warned about. It errs toward halting, which is the right direction,
  but it is the trade to revisit the next time a knob is added with a name close
  to an existing one.

## What is not on this list

Everything else the review found. Rows 1 to 11, 13 to 22, 24 to 29, 32, 36 and
38 are in 0.7.0, with a test group each that goes red when the guard is put back
the way it was.
